name: Build and Deliver

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21' # Update based on your Spring Boot requirements

      - name: Build Project with Maven
        run: mvn clean package -DskipTests
        env:
          MAVEN_OPTS: '-Xmx1024m'

      - name: Deliver Artifact to Runner
        run: |
          cp target/buttentica-0.0.1.jar /home/jeus/buttentica/buttentica.jar

      - name: Run Application with Secrets
        run: | 
           nohup java -jar /home/jeus/buttentica/buttentica.jar > /var/log/buttentica/output.log 2>&1 &D
        env:
          DB_PATH: ${{ secrets.DB_PATH }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}